package com.adobe.acs.commons.adobeio.core.config;

import org.osgi.service.metatype.annotations.ObjectClassDefinition;

@ObjectClassDefinition(name = "Adobe I/O. HTTP Client configuration (no configuration needed)", description = "Configuration of Adobe.io HTTP client service")
public @interface HttpClientConfiguration {
    // No properties are needed
}
